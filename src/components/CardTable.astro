---

---

<div class="table">
  <slot />
</div>

<style lang="scss">
  .table {
    margin: auto;
    display: grid;
    grid-gap: 1rem;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 320px), 1fr));
    max-width: calc(3 * min(100%, 320px) + 2 * 1rem);
  }
</style>
